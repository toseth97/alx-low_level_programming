#include "main.h"

/**
 * factorial - find a factorial of a given number
 * @n: interger to find its factorial
 * Return: Always 0
 */

int factorial(int n)
{
	int fac = 1;

	if (n > 0)
	{
		fact *= n;
		factorial(n - 1);
	}
	else
		return (-1);

	return (fact);
}
