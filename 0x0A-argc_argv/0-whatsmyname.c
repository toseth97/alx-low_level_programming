#include <stdio.h>
#include "main.h"

/**
 * main - print its name
 * @argc: number of aruments
 *
 * @argv: array pointing to argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
